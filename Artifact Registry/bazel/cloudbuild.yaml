# In this directory, run the following command to build this build step.
# gcloud builds submit --substitutions=_YYYYMMDD=$(date +%Y%m%d)

steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
  - 'build'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/bazel'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/bazel'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/bazel'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/bazel:${_YYYYMMDD}'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/bazel:${_YYYYMMDD}'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/bazel:${_YYYYMMDD}'
  - '.'
images:
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/bazel:latest'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/bazel:latest'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/bazel:latest'
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/bazel:${_YYYYMMDD}'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/bazel:${_YYYYMMDD}'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/bazel:${_YYYYMMDD}'
