# In this directory, run the following command to build this build step.
# gcloud builds submit --substitutions=_YYYYMMDD=$(date +%Y%m%d)

steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
  - 'build'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/npm'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/npm'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/npm'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/npm:${_YYYYMMDD}'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/npm:${_YYYYMMDD}'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/npm:${_YYYYMMDD}'
  - '.'
images:
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/npm:latest'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/npm:latest'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/npm:latest'
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/npm:${_YYYYMMDD}'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/npm:${_YYYYMMDD}'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/npm:${_YYYYMMDD}'
