# In this directory, run the following command to build this build step.
# gcloud builds submit --substitutions=_YYYYMMDD=$(date +%Y%m%d)

steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
  - 'build'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/helm'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/helm'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/helm'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/helm:${_YYYYMMDD}'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/helm:${_YYYYMMDD}'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/helm:${_YYYYMMDD}'
  - '.'
images:
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/helm:latest'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/helm:latest'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/helm:latest'
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/helm:${_YYYYMMDD}'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/helm:${_YYYYMMDD}'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/helm:${_YYYYMMDD}'
