# In this directory, run the following command to build this build step.
# gcloud builds submit --substitutions=_YYYYMMDD=$(date +%Y%m%d)

steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
  - 'build'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/git'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/git'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/git'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/git:${_YYYYMMDD}'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/git:${_YYYYMMDD}'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/git:${_YYYYMMDD}'
  - '.'
images:
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/git:latest'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/git:latest'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/git:latest'
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/git:${_YYYYMMDD}'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/git:${_YYYYMMDD}'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/git:${_YYYYMMDD}'
timeout: 900s
