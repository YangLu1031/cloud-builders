# In this directory, run the following command to build this build step.
# gcloud builds submit --substitutions=_YYYYMMDD=$(date +%Y%m%d)

steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
  - 'build'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/gradle'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/gradle'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/gradle'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/gradle:${_YYYYMMDD}'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/gradle:${_YYYYMMDD}'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/gradle:${_YYYYMMDD}'
  - '.'
images:
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/gradle:latest'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/gradle:latest'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/gradle:latest'
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/gradle:${_YYYYMMDD}'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/gradle:${_YYYYMMDD}'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/gradle:${_YYYYMMDD}'
