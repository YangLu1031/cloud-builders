# In this directory, run the following command to build this build step.
# gcloud builds submit --substitutions=_YYYYMMDD=$(date +%Y%m%d)

steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
  - 'build'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/dotnet'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/dotnet'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/dotnet'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/dotnet:${_YYYYMMDD}'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/dotnet:${_YYYYMMDD}'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/dotnet:${_YYYYMMDD}'
  - '.'
images:
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/dotnet:latest'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/dotnet:latest'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/dotnet:latest'
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/dotnet:${_YYYYMMDD}'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/dotnet:${_YYYYMMDD}'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/dotnet:${_YYYYMMDD}'
