# In this directory, run the following command to build this build step.
# gcloud builds submit --substitutions=_YYYYMMDD=$(date +%Y%m%d)

steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
  - 'build'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/kustomize'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/kustomize'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/kustomize'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/kustomize:${_YYYYMMDD}'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/kustomize:${_YYYYMMDD}'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/kustomize:${_YYYYMMDD}'
  - '.'
images:
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/kustomize:latest'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/kustomize:latest'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/kustomize:latest'
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/kustomize:${_YYYYMMDD}'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/kustomize:${_YYYYMMDD}'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/kustomize:${_YYYYMMDD}'
timeout: 900s
