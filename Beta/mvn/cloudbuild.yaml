# In this directory, run the following command to build this build step.
# gcloud builds submit --substitutions=_YYYYMMDD=$(date +%Y%m%d)

steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
  - 'build'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/mvn'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/mvn'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/mvn'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/mvn:${_YYYYMMDD}'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/mvn:${_YYYYMMDD}'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/mvn:${_YYYYMMDD}'
  - '.'
images:
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/mvn:latest'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/mvn:latest'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/mvn:latest'
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/mvn:${_YYYYMMDD}'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/mvn:${_YYYYMMDD}'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/mvn:${_YYYYMMDD}'
timeout: 900s
