# In this directory, run the following command to build this build step.
# gcloud builds submit --substitutions=_YYYYMMDD=$(date +%Y%m%d)

steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
  - 'build'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/kubectl'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/kubectl'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/kubectl'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/kubectl:${_YYYYMMDD}'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/kubectl:${_YYYYMMDD}'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/kubectl:${_YYYYMMDD}'
  - '.'
images:
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/kubectl:latest'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/kubectl:latest'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/kubectl:latest'
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/kubectl:${_YYYYMMDD}'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/kubectl:${_YYYYMMDD}'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/kubectl:${_YYYYMMDD}'
timeout: 900s
