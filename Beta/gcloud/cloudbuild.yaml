# In this directory, run the following command to build this build step.
# gcloud builds submit --substitutions=_YYYYMMDD=$(date +%Y%m%d)

steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
  - 'build'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/gcloud'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/gcloud'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/gcloud'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/gcloud:${_YYYYMMDD}'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/gcloud:${_YYYYMMDD}'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/gcloud:${_YYYYMMDD}'
  - '.'
images:
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/gcloud:latest'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/gcloud:latest'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/gcloud:latest'
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/gcloud:${_YYYYMMDD}'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/gcloud:${_YYYYMMDD}'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/gcloud:${_YYYYMMDD}'
timeout: 900s
