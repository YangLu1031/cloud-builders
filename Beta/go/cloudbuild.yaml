# In this directory, run the following command to build this build step.
# gcloud builds submit --substitutions=_YYYYMMDD=$(date +%Y%m%d)

steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
  - 'build'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/go'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/go'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/go'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/go:${_YYYYMMDD}'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/go:${_YYYYMMDD}'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/go:${_YYYYMMDD}'
  - '.'
images:
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/go:latest'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/go:latest'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/go:latest'
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/go:${_YYYYMMDD}'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/go:${_YYYYMMDD}'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/go:${_YYYYMMDD}'
