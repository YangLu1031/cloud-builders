# In this directory, run the following command to build this build step.
# gcloud builds submit --substitutions=_YYYYMMDD=$(date +%Y%m%d)

steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
  - 'build'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/javac'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/javac'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/javac'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/javac:${_YYYYMMDD}'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/javac:${_YYYYMMDD}'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/javac:${_YYYYMMDD}'
  - '.'
images:
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/javac:latest'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/javac:latest'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/javac:latest'
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/javac:${_YYYYMMDD}'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/javac:${_YYYYMMDD}'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/javac:${_YYYYMMDD}'
timeout: 900s
