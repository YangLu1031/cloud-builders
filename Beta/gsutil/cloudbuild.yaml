# In this directory, run the following command to build this build step.
# gcloud builds submit --substitutions=_YYYYMMDD=$(date +%Y%m%d)

steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
  - 'build'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/gsutil'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/gsutil'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/gsutil'
  - '--tag=us-docker.pkg.dev/$PROJECT_ID/cloud-builders/gsutil:${_YYYYMMDD}'
  - '--tag=europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/gsutil:${_YYYYMMDD}'
  - '--tag=asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/gsutil:${_YYYYMMDD}'
  - '.'
images:
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/gsutil:latest'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/gsutil:latest'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/gsutil:latest'
- 'us-docker.pkg.dev/$PROJECT_ID/cloud-builders/gsutil:${_YYYYMMDD}'
- 'europe-docker.pkg.dev/$PROJECT_ID/cloud-builders/gsutil:${_YYYYMMDD}'
- 'asia-docker.pkg.dev/$PROJECT_ID/cloud-builders/gsutil:${_YYYYMMDD}'
timeout: 900s
